<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SceneSeeker.Views.ItemsPage"
             Title="{Binding Title}"
             xmlns:local="clr-namespace:SceneSeeker.ViewModels"  
             xmlns:model="clr-namespace:SceneSeeker.Models"  
             x:Name="BrowseItemsPage">

    <!--
    
    In this file I simply experimented to see if there were any in-app based browser libraries already included with the project creation.
    Turns out, there is! So I will be using this to display the map in the app.
    
    -->

    <WebView x:Name="MapViewer" Source="https://planb.gay/ss.php"></WebView>
</ContentPage>
